example:
  property: "I AM IN THE DEFAULT FROM GITHUB!!!"

spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/eagle_eye_local
    username: postgres
    password: postgres